import React from 'react'
import clsx from 'clsx'

// styles
import styles from './_.module.scss'

const Header = () => (
  <header className={clsx('mab-1 pay-7', styles.header)} />
)

export default Header
